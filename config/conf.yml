---
defaults: &defaults
  output:
    output_dir: /opt/comfy/virtualmachines/
  packer_cache_dir: /opt/comfy/packer_cache/
  logging:
    log_level: ERROR
    #log_type: file # Two options: file, syslog. Defaults to stdout
    comfy_log_file: /var/log/comfy/comfy.log # Used when type file selected

########!!!!!!!DO NOT EDIT AFTER THIS POINT!!!!!!##########

production:
  <<: *defaults

development:
  <<: *defaults

