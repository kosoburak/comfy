---
defaults: &defaults
  output:
    output_dir: /opt/comfy/virtualmachines/
  logging:
    log_level: ERROR
    log_type: file # Two options: file, syslog. Defaults to stdout
    log_file: /var/log/comfy/comfy.log # Used when type file selected

########!!!!!!!DO NOT EDIT AFTER THIS POINT!!!!!!##########

production:
  <<: *defaults

development:
  <<: *defaults

